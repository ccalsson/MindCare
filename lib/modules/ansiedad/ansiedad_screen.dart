import 'package:flutter/material.dart';

class AnsiedadScreen extends StatelessWidget {
  const AnsiedadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Manejo de Ansiedad')),
      body: const Center(child: Text('Contenido de Manejo de Ansiedad')),
    );
  }
}

